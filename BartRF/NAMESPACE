useDynLib(BartRF, .registration = TRUE)

importFrom(Rcpp, evalCpp)

## If you truly want to export “everything alphabetic”, keep this.
## (Not recommended for CRAN-style packages, but you asked for full code.)
exportPattern("^[[:alpha:]]+")

importFrom(graphics, axis, points, segments)

importFrom(stats, as.formula, ave, median, qchisq, quantile, rnorm, sd)

## Fix the R CMD check NOTES:
importFrom(lme4, lmer, VarCorr)
importFrom(Matrix, bdiag)
importFrom(merTools, REsdExtract)
importFrom(truncnorm, rtruncnorm)
importFrom(nnet, class.ind)
